# Execution Plans

Create a plan for long tasks with objective, inputs, constraints, steps, gates, expected outputs, status, blockers, and decisions.

- Keep ongoing plans in `active/`.
- Move completed plans to `completed/` after final validation and delivery.
- Do not use plans as ceremonial status files for trivial tasks.
